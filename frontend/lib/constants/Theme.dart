import 'dart:ui' show Color;

class NowUIColors {
  static const Color black = Color(0xFF000000);

  static const Color white = Color(0xFFFFFFFF);

  static const Color primary = Color.fromRGBO(148, 210, 189, 1.0);

  static const Color tabs = Color.fromRGBO(0, 95, 115, 0.3);

  static const Color info = Color.fromRGBO(233, 216, 166, 1.0);

  static const Color text = Color.fromRGBO(0, 18, 25, 1.0);

  static const Color bgColorScreen = Color.fromRGBO(240, 249, 246, 1.0);

  static const Color border = Color.fromRGBO(186, 226, 213, 1.0);

  static const Color muted = Color.fromRGBO(187, 62, 3, 1.0);
  static const Color time = Color.fromRGBO(155, 34, 38, 1.0);
}
